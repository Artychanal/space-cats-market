spring:
  application.name: cosmo-cats
logging:
  level:
    root: INFO
    com.example.spacecatsmarket: DEBUG
  pattern.console: "%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n"
clients:
  rates:
    base-url: http://localhost:8089
    connect-timeout-ms: 1000
    read-timeout-ms: 1500
